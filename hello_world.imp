@import "stdio.h";

_() main: {
    @array int evens: |2 4 6 8 10|;
    
    loop (i < evens.len) [+] {
        int current: evens.data[i];
        evens.data[i]: [current - 2];
    }

    @c [printf("%d\n", evens.data[4]);];
}
