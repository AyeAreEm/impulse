@import "stdio.h";

struct[T] vector: {
   $T x;
   $T y;
}

vector(typeid T $T x_pos $T y_pos) vector_new: {
    vector[T] new;
    new.x: x_pos;
    new.y: y_pos;
    return new;
}

_() main: {
    vector[int] pos: vector_new(int 10 15);

    @c [printf("x: %d, y: %d\n", pos.x, pos.y);];
}
