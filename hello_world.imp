@import "time.h";
@import "base/random.imp";

int main :: () {
    @c [srand(time(NULL));];

    int min: 0;
    int max: 100;

    loop (i < 10) [+] {
        int randnum: random.range(min max);
        println("%d" randnum);
    }
}
