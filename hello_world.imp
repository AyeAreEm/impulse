@import "base/string.imp";

_ main :: () {
    string word: string.from("hello there");
    defer string.dealloc(&word);

    for (word) [ch i] {
        defer println("%zu" i);
        println("%c" ch);
    }
}
