struct vec :: {
    int x;
    int y;
}

vec vec.new(int x int y) :: {
    vec new;
    new.x: x;
    new.y: y;
    return new;
}

_ main() :: {
    vec pos :: vec.new(10 15);
    pos.x: 20; # this will error since pos is constant
}
