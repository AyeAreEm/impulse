@import "stdio.h";
@import "string.h";

^char(typeid T) type: {
    @c [#T;];
}

int(^char str1 ^char str2) str_compare: {
    @c [return strcmp(str1, str2);];
}

bool(typeid T typeid K) type_compare: {
    return str_compare(type(T) type(K));
}

#_($T var) example: {

#}

struct vector: {
    blah x;
    blah y;
}

_() main: {
    vector pos;
}
