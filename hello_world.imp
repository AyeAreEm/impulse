@import "stdio.h";

_(bool original) greet: {
    if (!original) {
        @c [printf("hello there?\n");];
        return;
    }

    @c [printf("hello world\n");];
}

_() main: {
    bool norm: true;

    greet(norm);
    greet(false);
}
