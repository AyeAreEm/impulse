_ main() :: {
    # funcs and struct need :: now since they are constant

    int x: 10;
    if (x = 10) {
        loop (i < x) [+] {}
    } orif (x = 9) {}
    # fixed bug, it would say that "x" on line 7 was undefined. now works
}
