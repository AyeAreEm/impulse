@import "base/string.imp";

_ main :: () {
    string word: string.from("hello world");

    defer {
        if (word.len = 11) {
            println("it is 11 letters long");
        } else {
            println("what how");
        }
    }

    println("%s" word.data);
}
