@import "stdio.h";

struct[T K] tuple: {
    $T one;
    $K two;
}

_() main: {
    tuple[u8 i8] test;
    test.one: 256;
    test.two: 128;

    @c [
        printf("one: %d\n", test.one);
        printf("two: %d\n", test.two);
    ];
}
