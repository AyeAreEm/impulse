@import "base/io.imp";

_ main :: () {
    string word: string.from("hello world");
    char exclaim;
    @c [exclaim = '!';];

    string.push_char(&word exclaim);
    string.push_cstr(&word " goodbye.");
    @c [printf("%s\n", word.data);];

    mem.dealloc(word.data);
}
