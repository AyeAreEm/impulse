@import "stdio.h";

_(typeid T) check_size: {
    @c [printf("%zu\n", sizeof(T));];
}

struct vector: {
    int x;
    int y;
}

_() main: {
    check_size($i32);
    check_size($char);
    check_size($i8);
    check_size($u8);
    check_size($vector);
}
