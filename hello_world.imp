@import "base/string.imp";

_ main :: () {
    string word: string.from("hello world");

    defer {
        for (word) [ch] {
            println("%c" ch);
        }
        string.dealloc(word);
    }

    println("%s" word.data);
}
