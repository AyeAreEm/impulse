struct[T] Node :: {
    $T value;
    ^Node next;
}

_ main :: () {
    Node[int] first;
    Node[int] second;
    second.value: 15;

    first.value: 10;
    first.next: &second;

    ^Node[int] n: first.next;
    int sv: n.value;
    println("%d" sv);
}
