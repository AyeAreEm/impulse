@import "base/dynamic.imp";

_ main :: () {
    let x: 10;
    dyn[int] dec_dyn_int;
    let nums: dyn.new(int); # this doesn't work without above cuz generator checks if the type has been made already
}
