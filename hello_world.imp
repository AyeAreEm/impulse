@import "stdio.h";

struct[T] vector: {
   $T x;
   $T y;
}

_(typeid T) show_size: {
    @c [printf("size: %zu", sizeof(T));];
}

_() main: {
    vector[u8] pos;
    pos.x: 10;
    pos.y: 100;

    @c [printf("pos x: %d, pos y: %d", pos.x, pos.y);];
}
