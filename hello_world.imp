# updated most c functions used to c.<func_name>
# updated raylib function GetFileExtension to actually return the result
# updated Time.sleep function to use _sleep() vs Sleep() on windows to avoid name collisions with raylib
