@import "base/raylib/raylib.imp";

_ main :: () {
    rl.InitWindow(800 600 "Raylib - Impulse");

    loop (!rl.WindowShouldClose()) {
        rl.BeginDrawing();
        rl.ClearBackground(rl.RED);
        rl.EndDrawing();
    }

    rl.CloseWindow();
}
