@import "base/string.imp";

_ main :: () {
    string word: string.from("hello world");

    defer {
        loop (i < word.len) [+] {
            char ch: word.data[i];
            println("%c" ch);
        }
        string.dealloc(word);
    }

    println("%s" word.data);
}
