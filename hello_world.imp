@import "base/dynamic.imp";

_ main :: () {
    let nums: dyn.new(int);

    loop (i < 10) [+] {
        dyn.push(int nums [i * 10]);
    }

    for (nums) [n i] {
        println("%zu: %d" i n);
    }
}
