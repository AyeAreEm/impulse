struct vector :: {
    int x;
    int y;

    vector new :: (int x int y) {
        vector new;
        new.x: x;
        new.y: y;
        return new;
    }
}

struct tuple :: {
    int left;
    int right;
}

int main :: () {
    vector x: vector.new(10 15);
    tuple y: cast_struct(vector x tuple);

    @c [printf("y.left: %d, y.right: %d", y.left, y.right);];
}
