@import "base/string.imp";
@import "stdio.h";

char() c_getchar: {
    @c [return getchar();];
}

_(string str) str_println: {
    loop (i < str.len) [+] {
        @c [printf("%c", str.data[i]);];
    }

    @c [printf("\n");];
}
