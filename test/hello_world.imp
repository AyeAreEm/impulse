_ change :: (^int num) {
    @mut num; # this makes it mutable or errors if expression is actually const
    num^: 10;
}

_ main :: () {
    int x: 1;
    ^int y :: &x; # impulse treats this as a constant pointer to a constant value

    change(y); # error, y is constant when the argument is mutable
}
