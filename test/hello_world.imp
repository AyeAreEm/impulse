@import "base/random.imp";

_ main :: () {
    let MT_rng: MT.init(Time.since_epoch());
    random LCG_rng: @default;

    loop (i < 10) [+] {
        let MT_num: MT.range(&MT_rng 0 100);
        let LCG_num: random.range(&LCG_rng 0 100);
        println("MT: %d, LCG: %d" MT_num LCG_num);
    }
}
