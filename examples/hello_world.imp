# currently no in house support for print formatting in impulse
@import "base/string.imp";

_() main: {
    # working print without formatting in impulse
    string hello: str_from("hello world");
    str_println(hello);

    # c embed version
    @c [ printf("hello world"); ];
}
